name: publish-x86-image

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*-x86-image'

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Normalize GHCR image owner
        id: ghcr-owner
        run: |
          owner="${{ github.repository_owner }}"
          echo "owner=${owner,,}" >> "$GITHUB_OUTPUT"

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/pwn-x86.Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ steps.ghcr-owner.outputs.owner }}/pwnsolver-x86:latest
            ghcr.io/${{ steps.ghcr-owner.outputs.owner }}/pwnsolver-x86:${{ github.sha }}
